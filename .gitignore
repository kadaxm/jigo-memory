# secrets — never commit
.env

# local vector DB (machine-specific)
chroma_memory/

# audio artifacts (incl. personal voice samples)
*.wav

# training data built from personal vault notes — never public
salience_dataset.json

# runtime logs
*.log
*.err

# local backups
*_backup.pt
*_backup.json

# side venv (heavy, machine-specific)
xtts_env/

# interview prep + mission context — private, never commit
TRAINING_NOTES.md
MISSION_NOTES.md

# python cruft
__pycache__/
.venv/

# benchmark audio bulk (kept local, data files committed)
benchmarks/gujarati_indicemo_lite/audio/
benchmarks/gujarati_indicemo_lite/drift_audio/
benchmarks/gujarati_indicemo_lite/xtts_audio/
